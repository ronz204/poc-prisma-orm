model Game {
  id        Int    @id @default(autoincrement())
  title     String @unique
  genre     String
  developer String
  publisher String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("games")
  @@schema("esports")
}
